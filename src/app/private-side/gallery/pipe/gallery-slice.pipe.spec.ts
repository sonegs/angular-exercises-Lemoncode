import { GallerySlicePipe } from './gallery-slice.pipe';

describe('GallerySlicePipe', () => {
  it('create an instance', () => {
    const pipe = new GallerySlicePipe();
    expect(pipe).toBeTruthy();
  });
});
